version: "3.8"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    restart: unless-stopped
    # Uncomment for development with hot-reload (requires additional Dockerfile changes)
    # volumes:
    #  - ./src:/app/src
    #  - ./public:/app/public
