import FortuneWheel from "./FortuneWheel";
// import { useParams } from "react-router-dom";

const Play = () => {
  // const { shopId } = useParams();

  // fetch Shop

  return <FortuneWheel />;
};

export default Play;
