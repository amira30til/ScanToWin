## Run project

docker-compose up

- For deployement uncomment `# COPY nginx.conf /etc/nginx/conf.d/default.conf` from the `Dockerfile`

## Run dev

- pnpm dev

## Hard coded Values

- FORTUNE_WHEEL_NAME - used in

  - pages/user/index.jsx

- ADMIN_ROLE
- SUPER_ADMIN_ROLE
  - app.jsx
